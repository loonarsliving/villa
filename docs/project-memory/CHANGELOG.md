# CHANGELOG.md

Built entirely from `git log` on `main` (branch `claude/project-memory-audit-af4m1t`, based on `main`@`ab473b3`). No entries are fabricated; dates are commit dates from git.

## main branch history (oldest → newest)

### 2026-06-13
- `18977d1` — Add files via upload (initial commit / project bootstrap).

### 2026-08-04
- `17ebbd8` — security: fix hardcoded admin token + add `.gitignore`.

### 2026-08-09
- `f2ffcfb` — Rombak sistem: dashboard Investor/Manajemen/Resepsionis + integrasi Cloudbeds & WA (system overhaul: Investor/Management/Receptionist dashboards + Cloudbeds & WhatsApp integration).
- `bc74d1b` — Migrate villa frontend from static HTML to Next.js.
- `6e47839` — Port must-change-password flow, Cloudbeds mapping UI, and other improvements found in a concurrent HTML-based commit on this repo.
- `8b2524a` — Merge origin/main (concurrent HTML dashboard work) into Next.js migration.
- `d203c7f` — chore: trigger redeploy after fixing Vercel project framework setting.
- `2ddcff5` — Move Cloudbeds webhook to Vercel, remove DB-backed integration settings.
- `346ab86` — Cegah double-booking berbasis tanggal di Front Desk (prevent date-based double booking).

### 2026-08-18
- `932f6de` — Redesign UI to a light, colorful mobile-style theme.
- `54fc066` — Merge: redesign UI to light, colorful mobile-style theme.
- `ab473b3` — chore: ignore tsbuildinfo artifact. *(current `main` tip)*

## Unmerged branch history (not part of `main`, listed for reference only)
- `claude/repo-overview-m1rcy2`: `01bcad3` Pindahkan kredensial Cloudbeds & WhaCenter ke Vercel Environment Variables; `71552b2` Tambah panel Super Admin, integrasi Cloudbeds (OTA) & WhaCenter (WA+PIN kamar).
- `claude/villa-system-no-receptionist-cjofxy`: `d77590a` Rombak sistem: hapus resepsionis, tambah dashboard investor & admin; `1f6a371` Rancang ulang: resepsionis tetap ada, Cloudbeds cuma channel manager; `74daad8` Tambah form data investor (nama+HP) setelah ganti password pertama.
- `claude/security-audit-repos-4cs6h7`: `d5dcf97` security: use real per-session token on owner/receptionist dashboards.
- `claude/file-hub-repo-integration-7nm114`: `32d6ff5` Version villa-api in-repo, add /bridge/occupancy, log real WA send results.
- `claude/frigate-ai-cctv-module-eqwuri`: `db248d5` Add AI CCTV module: EZVIZ checkpoint snapshots + Gemini Vision presence detection; `b2fa553` Route AI CCTV vision detection through Mkhsistem's Gemini instead of its own.
- `claude/security-3-repos-tj69ek`: `1657dfb` Harden client-side role gating with server-side proxy + gate data fetch on auth ready; `a6c853b` Validate Cloudbeds webhook reservation payload before writing to Supabase; `341ac2f` Upgrade Next.js 15.1 -> 16.3.1 to fix known postcss/sharp CVEs.

## This audit
- `claude/project-memory-audit-af4m1t` — added `/docs/project-memory/` and `/CLAUDE.md` (this documentation set). Audit-only; no source code, database, or deployment changes made.

### 2026-08-23 — `claude/payment-gateway-admin-module-2t1mq9`
- Added Payment Gateway module (`admin/payment-gateway/page.tsx`) — admin-only walk-in cafe/spa cashier with an extra email-whitelist gate, static-QRIS display, and transaction history.
- Resolved the `villa-api` source-location mystery: confirmed live only as a deployed Supabase Edge Function (not in this repo, not in `mkhsistem`), read/redeployed directly via Supabase MCP.
- Confirmed the shared Supabase project relationship with `mkhsistem` (MK Connect) described in PROJECT_CONTEXT.md/CURRENT_STATE.md as previously unconfirmed.
- Added `walkin_payments` table (migration `add_walkin_payments_table`) and three new `villa-api` endpoints (`GET/POST/PATCH /admin/walkin-payments`, deployed as villa-api v14) — first production DB schema change and first `villa-api` redeploy made from this repo's git history.
- Surfaced (not remediated) a pre-existing, unrelated security advisory: 2 tables in the shared project have RLS fully disabled (`istri_daily_tips`, `contractor_fund_request_pending`).
- Follow-up same day: moved the QRIS image from localStorage into `integration_settings` (key `walkin_qris`, via existing `/admin/settings` endpoints — no schema change needed); added a read-only `walkin_income` breakdown to `GET /report` (villa-api v15) and a matching info card on the investor dashboard, at the owner's request, so investors can see cafe/spa walk-in income even though it stays out of the 70/30 split. Merged to `main` and deployed to production same day.
- Second follow-up same day: added a "Villa" option to the Payment Gateway kasir for walk-in guests who book directly instead of via OTA/Cloudbeds — deliberately wired into the real booking pipeline (`POST /bookings` → `POST /checkin`) rather than the `walkin_payments` table, since villa rental income must feed the 70/30 investor split (unlike cafe/spa). Added `PATCH /bookings` (villa-api v16) to cancel an unpaid walk-in booking. Merged to `main` and deployed to production same day.
- Third follow-up same day, in response to an owner request to confirm and correct the 3-role capability map:
  - Relocated Payment Gateway from `admin/payment-gateway` to `front-desk/payment-gateway` (villa-api v17 renamed its endpoints off `/admin/*` to `/walkin-payments` and `/walkin-qris`) so receptionist gets the same one feature directly, no email gate; the gate still applies for admin.
  - Made investor (`role=owner`) reporting collective/villa-wide instead of per-unit, per explicit instruction that unit ownership is only a stay-rights token, not a revenue-share basis: `/report`, `/transactions`, `/notifications` no longer scope to the investor's own `unit_id` (villa-api v17). Added `investor_count`/`per_investor_amount` to `computeReport()`, and in v18 corrected the Rp 5jt/month minimum-guarantee check to compare against `per_investor_amount` (per investor) instead of the whole pool — a bug that would have been introduced by the collective-view change if left uncaught.
  - Confirmed CCTV visibility for admin is NOT implemented on `main` (only on the unmerged, unaudited `claude/frigate-ai-cctv-module-eqwuri` branch) — owner asked for that branch to be reviewed (not merged) as a separate step. Audit found: branch diverged from `main` before the entire current UI theme and today's other changes (merging as-is would revert 61 files, including deleting Payment Gateway); its CCTV access check is hardcoded to one specific user id ("Avianto Perdana"), not role-based; needs EZVIZ developer credentials + per-camera serial/verification code; AI vision calls out to Mkhsistem's Gemini bridge; its `cctv_cameras`/`cctv_checkpoint_log` tables already exist in the shared DB from an earlier session but are empty.
- Fourth follow-up same day: removed the email-whitelist super-admin gate (`AccessGate`, `src/lib/superAdmin.ts`) from Payment Gateway entirely, per owner instruction that it's unnecessary now that role gating (`admin`/`receptionist` via `FrontDeskLayout`) is properly in place — access is by role alone for both roles.

### 2026-08-27 — `claude/villa-repo-construction-mapping-pi2uat`
- Prepared the repo for a Cloudbeds API key (self-service, property-level `x-api-key` auth per Cloudbeds' own docs) ahead of the owner adding one: new `src/lib/cloudbedsApi.ts` client + `GET /api/admin/cloudbeds/rooms` route calling Cloudbeds' `getRooms` endpoint.
- Upgraded the admin Cloudbeds mapping page (`src/app/admin/cloudbeds/page.tsx`) with a live room picker sourced from that route, falling back to the existing manual Room ID/Name inputs when `CLOUDBEDS_API_KEY` is unset or the call errors.
- Added `.env.example` documenting all four Cloudbeds/Supabase env vars (no values) — closes a previously documented gap.
- No database schema change, no change to the external `villa-api` Edge Function, no change to how mappings are stored.

### 2026-08-27 (same session) — traced `villa-api` source directly via Supabase MCP, at owner's prompt to verify before continuing
- Read the live deployed `villa-api` v18 source (`get_edge_function`) and confirmed several previously "UNKNOWN"/inferred facts: real server-side session auth + role authorization exists in `villa-api` (not just client-side gating); `villa-api` has its own second, currently-unconfigured Cloudbeds webhook (`POST /webhook/cloudbeds`, DB-stored secret, no `integration_settings` row exists for it — dead code path, this repo's own env-var-based webhook is the live one); WhatsApp sending is not "WhaCenter" but a proxy (`sendWa()` → `integration_settings.vercel_bridge`) to the sibling Mkhsistem system's `/api/wa/send` — confirmed `vercel_bridge.base_url = https://mkh.haluoleo.id` by direct query.
- Updated ARCHITECTURE.md, INTEGRATIONS.md, PROJECT_CONTEXT.md, CURRENT_STATE.md to record these as confirmed, not inferred. No code changes in this step — documentation only.

### 2026-08-27 (same session) — owner-approved villa-api cleanup + Cloudbeds room-picker hardening
- Owner explicitly approved removing the dead, never-configured `POST /webhook/cloudbeds` endpoint from `villa-api` (confirmed unconfigured in the prior step). Deployed `villa-api` v19 with that endpoint deleted; nothing else changed. Verified post-deploy via `list_edge_functions`/`get_edge_function` (status ACTIVE, `verify_jwt: false` preserved). `GET /admin/cloudbeds/log` and `GET/POST/DELETE /admin/cloudbeds/mapping` (which read/write the same `cloudbeds_events_log`/`cloudbeds_room_mapping` tables) are untouched.
- Added a standing rule to `CLAUDE.md`: villa-api behavior must be verified via Supabase MCP before being documented, not inferred — to stop the class of documentation errors found this session (wrong claims about server-side auth and WhatsApp sending).
- Hardened `/api/admin/cloudbeds/rooms` (added 2026-08-27 earlier this session): it was reachable by anyone who could hit the Vercel deployment, with no auth check of its own. Now requires a valid admin `x-villa-token`, verified by forwarding it to villa-api's own `GET /admin/overview` (admin-only) rather than re-implementing token verification in this repo. `src/lib/api.ts` now exports `getToken()` so the admin Cloudbeds page can attach the header. This is the last piece needed before the owner adds `CLOUDBEDS_API_KEY` tomorrow — once that env var is set in Vercel, the live room picker should work end-to-end with no further code changes.

### 2026-08-27 (same session) — real dynamic QRIS for Payment Gateway via iPaymu
- Owner clarified Payment Gateway's existing QRIS "feature" was just an admin-uploaded static photo, no real QRIS logic. Researched Indonesian QRIS PSPs against the owner's criteria (easy registration, fastest settlement); Tripay was the initial pick but is currently closed to new merchant registration, so iPaymu was chosen instead (KTP-only registration, supports both static and dynamic QRIS via API).
- Verified iPaymu's outbound API v2 contract directly against their official `ipaymu-php-api` GitHub source (not guessed): base URLs, `/payment/direct` + `/transaction` endpoints, and the exact HMAC-SHA256 signature algorithm. Could NOT verify the `/payment/direct` response shape or the v2 webhook signature scheme — `docs.ipaymu.com` and their signature PDF were unreachable (network egress blocked in this dev environment) — documented as an open item rather than guessed.
- Added `src/lib/ipaymuApi.ts` (client), `/api/payment-gateway/qris` + `/api/payment-gateway/qris/status` routes (staff-gated via the same villa-api-forwarding pattern as the Cloudbeds rooms route — extracted into shared `src/lib/villaApiAuth.ts`), and `/api/webhooks/ipaymu` (inbound notify receiver).
- Webhook trust model: does not trust the inbound callback's payload/signature (unverified for v2) — treats it only as a trigger to independently ask iPaymu ourselves (our own signed `checkTransactionStatus` call), and only that response can mark a transaction paid. This sidesteps the unverified inbound signature entirely rather than guessing at it.
- Scope, per explicit owner decision: only `walkin_payments` (cafe/spa/lainnya) auto-flip to `lunas` from the webhook. Villa bookings still require a manual "Tandai Lunas & Check-In" click (now paired with a live "Cek Status" iPaymu check in the same modal) because check-in has side effects (WA PIN send, 70/30 split transaction recording, unit status change) the owner wants a staff decision on, not full automation.
- Added `qrcode` npm dependency (renders a raw QRIS payload into a scannable image when iPaymu doesn't return a ready image URL). No database schema change — matches on `walkin_payments.id` via a `walkin_<uuid>` referenceId convention, no new table.
- Inert until `IPAYMU_VA`/`IPAYMU_API_KEY` are set (falls back to the pre-existing static-image flow) — see DEPLOYMENT.md's new activation checklist, which explicitly calls out that the response-shape assumption needs a real sandbox test before production use.

### 2026-08-27 (same session) — real booking calendar for Front Desk + Admin, ahead of Cloudbeds go-live
- Owner asked for the interface to be readied for Cloudbeds integration on both admin and receptionist sides, specifically calling out that receptionist needs a booking calendar (not just check-in) so OTA data is visible realtime. Studied Cloudbeds' own calendar UI (unit rows × date columns, colored bars per booking, OTA/unassigned indicators) via their public docs before designing villa's version, then confirmed scope with the owner: read-only visual grid first (no drag-drop reassignment yet), shared between Front Desk and Admin nav.
- villa-api v20 (deployed, owner-approved): added optional `date_from`/`date_to` to `GET /bookings`, backward-compatible, reusing the existing `datesOverlap()` helper.
- Replaced `front-desk/booking/page.tsx`'s flat 50-row list with a real calendar: units grouped by block as rows, a scrollable date range (7/14/21 days, prev/next/today navigation) as columns, bookings rendered as bars (color = status, ☁ prefix = Cloudbeds/OTA source) using percentage-based absolute positioning (CSS Grid auto-placement can't reliably overlay a variable-span bar on fixed per-day cells in the same row, so this was deliberately not built as a grid overlay). Cloudbeds bookings need no extra sync work — they already land in `bookings` in realtime via the existing webhook.
- Added the same page to Admin's nav (new "Operasional" section) per owner's explicit choice that admin should see the same calendar, not just receptionist.
- No other admin interface changes were made this round — owner chose "just add the shared calendar," not a broader admin visual redesign.

### 2026-08-27 (same session) — fix duplicate-booking-conflict bug, lock walk-in tarif to unit rate, payment-before-checkin for walk-in
- Owner asked to confirm OTA auto-fill and walk-in-can't-pick-an-OTA-taken-unit behavior (both confirmed true by reading the code, no changes needed there), then flagged two real gaps: walk-in tarif shouldn't be freely typed by staff, and clicking Check-In should show payment first before check-in data is committed.
- Investigating surfaced a third, unprompted bug: the Front Desk dashboard's "+ Check-In" modal always called `POST /bookings` regardless of whether a matching booking already existed — meaning trying to check in a guest whose booking was already auto-synced from Cloudbeds would 409-conflict. Owner approved fixing this in the same pass.
- `front-desk/page.tsx`'s Check-In modal is now two tabs: "Booking Terjadwal" (pick an existing booking, any source, direct check-in, no payment — it's already paid via that channel) and "Tamu Baru" (no existing booking — Walk-in hands off to Payment Gateway for QRIS-first payment before check-in commits; any other source keeps manual create+checkin with manually-entered tarif, since that's an externally-set OTA price this repo can't know automatically).
- `front-desk/payment-gateway/page.tsx`'s Villa tarif field is now read-only, computed from the selected unit's own `tarif_harian`/`tarif_bulanan` (added to the `Unit` frontend type — the columns already existed in the DB/API, just weren't declared/used in this repo). Also reads `?kategori=villa` to support the Check-In modal's handoff.
- No villa-api changes, no database schema changes — both fixes are frontend-only, using data villa-api already returned.

### 2026-08-27 (same session) — Check-In Card (KTP photo + digital signature), Tahap 1 of a bigger OCR/Filemanager plan
- Owner asked to make walk-in check-in easy via a signable "check-in card," and to also make OTA check-in easier the same way. Follow-up asked for KTP OCR + storage in the separate "Filemanager" (Ultron) app bridged through Mkhsistem.
- Researched before building (per the standing villa-api-verification rule, extended here to Mkhsistem/Filemanager): read Mkhsistem's `app/api` tree directly — confirmed no KTP-OCR endpoint exists there, and that "Filemanager" is a fully separate Vercel deployment ("Ultron", `filemanager.haluoleo.id`) whose only confirmed relationship to Mkhsistem is that IT calls INTO Mkhsistem's AI (`/api/ai/voice-bridge`), not the reverse. Filemanager's own repo was not added to this session's access. Given this spans 3 production apps and 2 repos outside this session's current access, owner agreed to sequence it: Tahap 1 (villa-only, this round) now, OCR + Filemanager bridge later as a separate piece of work.
- Migration `add_checkin_card_ktp_signature`: `bookings.ktp_photo_path`, `bookings.signature_data_url` (both nullable text), plus a new private Storage bucket `guest-documents` (public: false, matches the `attendance-selfies`/`memo-attachments` precedent in this shared project — KTP photos are sensitive PII).
- villa-api v21: `POST /checkin` now persists both fields from the request body; omitting them behaves exactly as before (purely additive).
- Added `src/components/CheckinCard.tsx` (shared, canvas-based signature pad using pointer events + a camera-capable file input for the KTP photo) and `/api/checkin/upload-ktp` (staff-gated, service-role upload to the new bucket, returns only the storage path — never a public URL).
- Wired into all three places a booking actually flips to `checkin`: Front Desk's "Booking Terjadwal" tab (existing/OTA bookings), Front Desk's "Tamu Baru" non-walk-in tab (manually-logged external-channel bookings), and Payment Gateway's "Tandai Lunas & Check-In" for Villa walk-ins. All three now open the Check-In Card and only call `/checkin` after a photo + signature are captured, instead of calling it immediately.
- No viewer UI was built for looking at a stored KTP photo/signature afterward — out of scope for Tahap 1, noted in ROADMAP.md as a likely Tahap 2/3 need.

### 2026-08-27 (same session) — Cloudbeds connection test card
- Owner asked how to verify the just-redeployed `CLOUDBEDS_API_KEY` was actually working, since no Vercel MCP tool can read env var values directly. Rather than only pointing at browser DevTools, replaced `admin/cloudbeds/page.tsx`'s passive status banner with a dedicated "Tes Koneksi Cloudbeds" card: manual "Tes Ulang" button, a clear connected/disconnected/testing badge, last-checked timestamp, and (on failure) a specific list of likely causes (key unset/misnamed, key invalid/expired at Cloudbeds, or `CLOUDBEDS_PROPERTY_ID` needed for a multi-property account) instead of a raw error string.
- Frontend-only change, no villa-api/database changes — reuses the existing `/api/admin/cloudbeds/rooms` route.

### 2026-08-28 — Amenities stock module, auto-linked to OTA booking housekeeping tasks
- Owner asked: when an OTA booking notification lands, housekeeping should also get a task to complete/restock room amenities, and finishing that task should deduct stock from a new Amenities catalog visible to both Admin and Reception.
- Confirmed 2 design decisions via `AskUserQuestion` before touching the schema (per CLAUDE.md's "never change database schema without approval"): stock deduction uses a standard "kit" (fixed qty per item, one click) rather than a manual per-task form, and only Admin can write to the catalog/kit — Reception is read-only.
- Migration `add_amenities_stock_and_kit`: new tables `amenities` (catalog + stock + stock_minimum), `amenity_kit_items` (the standard kit — one global kit, not per unit-type, since `units` has no room-type/category column), `amenity_usage_log` (audit trail); `housekeeping` gains a `jenis` column (`'bersih'` default / `'amenities'`), fully additive.
- villa-api v22: `GET /amenities`, `/amenities/kit`, `/amenities/usage-log` open to staff (admin+receptionist); `POST/PATCH/DELETE` on `/amenities` and `/amenities/kit` restricted to admin. `PATCH /housekeeping/done` now branches on the task's `jenis` — `'amenities'` deducts kit stock + logs usage instead of flipping the unit to `available` (that stays exclusive to `'bersih'` checkout tasks); everything else behaves exactly as before.
- Villa's own Cloudbeds webhook route now also inserts a `jenis:'amenities'` housekeeping task (alongside its existing notification) whenever a new OTA reservation matches a mapped unit — surfaces in the existing Housekeeping checklist with a 🧴 marker, no new notification channel needed.
- New pages `admin/amenities` (manage catalog/kit/restock/usage log) and `front-desk/amenities` (read-only stock + usage log), with nav links added to both Admin and Front Desk shells.

### 2026-08-28 (same session) — WA alert to housekeeping + reception on OTA booking
- Owner wanted an actual WhatsApp alert (not just the existing in-app notification) sent to housekeeping and reception when an OTA booking lands, so housekeeping can prep the room and reception can prepare to greet the guest.
- Checked Mkhsistem's employee schema live via Supabase MCP before building: `positions` are generic org levels (Direktur/Manager/Staff/Supervisor), not job functions — `divisions` (free-text, admin-editable) is the right table to tag "Housekeeping Villa" on. No such division exists yet; the owner needs to create it in Mkhsistem and assign the employee(s) to it.
- `src/app/api/webhooks/cloudbeds/route.ts` gained a local `sendWaBridge()` (duplicates villa-api's `sendWa()` contract — same `vercel_bridge` setting/endpoint/logging — since this Vercel route and villa-api's Deno Edge Function can't share source) and, on a matched OTA reservation, sends WA to: housekeeping (phone from Mkhsistem's `employees` joined `divisions.name ILIKE 'Housekeeping Villa'`, `employment_status='active'`) and reception (phone from villa's own `villa_users`, `role='receptionist'`, `is_active`).
- No database schema changes — read-only lookups against existing tables in both villa and (cross-project, same Supabase instance) Mkhsistem.

### 2026-08-28 (same session) — reworked check-in flow: pure walk-in tab, KTP+signature before payment
- Owner walked through the actual check-in flow for OTA and walk-in guests and asked to verify it against the code. OTA ("Booking Terjadwal" tab) already matched: click a name (unit already locked in via Cloudbeds mapping, no manual picking) → Check-In Card (KTP+signature) → checkin commits. No changes there.
- "Tamu Baru" had two real gaps, both fixed with explicit owner approval (confirmed via `AskUserQuestion` to remove, not relocate, the manual multi-source entry — CLAUDE.md requires approval before deleting working functionality): (1) it still showed a "Sumber Booking" dropdown with a manual-entry form for Airbnb/Tiket/Agoda/Website/WhatsApp — removed entirely, "Tamu Baru" now means walk-in only, no choice, straight hand-off to Payment Gateway; (2) walk-in's order was backwards from what the owner wants — it used to pay via QRIS first, then capture KTP+signature only after marking lunas.
- Reworked `front-desk/payment-gateway/page.tsx` so capture happens **before** the booking/QRIS exist: fill guest+unit form → Check-In Card (KTP+signature) → confirming creates the booking and shows the QRIS → once marked lunas, `/checkin` fires immediately using the already-captured KTP+signature (no second prompt). Added a same-session fallback (`checkinCardMode: "existing"`) that re-prompts for capture if that in-memory data was lost (e.g. a page refresh between capture and payment), so check-in can never commit without KTP/signature on file.
- No villa-api or database changes — frontend-only reordering/simplification of existing calls.

### 2026-08-28 (same session) — CCTV live-view via EZVIZ, admin-only
- Owner activated their EZVIZ CCTV and wants to watch cameras through villa's own interface. Confirmed scope via `AskUserQuestion`: live-view only (not the older unmerged AI-Gemini-presence-detection branch, `claude/frigate-ai-cctv-module-eqwuri`, left as-is for later), admin-only access.
- Researched EZVIZ's real live-view contract before building (`open.ys7.com`'s own docs are network-egress-blocked, so via WebSearch + GitHub source instead): the supported way to embed live video in a web page is EZVIZ's own `ezuikit-js` SDK (`EZUIKitPlayer({id, accessToken, url: 'ezopen://[verificationCode@]open.ys7.com/{serial}/{channel}.live', env:{domain}})`) — not a plain `<video>` tag.
- villa-api v23: `GET/POST/PATCH/DELETE /admin/cctv/cameras` (metadata only — nama, deskripsi, ezviz_serial, ezviz_channel_no, ezviz_verification_code, is_active — no secrets). `EZVIZ_APP_KEY`/`EZVIZ_APP_SECRET` live only as Vercel env vars (same pattern as `CLOUDBEDS_API_KEY`); `src/lib/ezviz.ts` (server-only) fetches/caches the account-level access token, and the new admin-gated `/api/cctv/token` route hands that token to the browser for EZUIKit — required by EZVIZ's own SDK design, the APP_KEY/SECRET themselves never leave the server.
- Discovered mid-build that `cctv_cameras` already existed in the shared DB (empty, created in an earlier session for the older AI-detection branch) with a different shape (`zona` NOT NULL, `deskripsi_titik`). Reconciled with a small migration (`deskripsi_titik`→`deskripsi`, `zona` made nullable) instead of creating a duplicate table — safe since the table held zero rows.
- New `admin/cctv` page: camera CRUD + a "Tonton" live-view modal that dynamically imports `ezuikit-js` client-side. Nav link added to `AdminShell`'s Integrasi section.

### 2026-08-30/31 (same session) — EZVIZ token/get fixed, HLS endpoint, AI checkpoint module (satpam/resepsionis)
- Live-view kept failing in production ("AppKey doesn't exist") despite curl-verified-correct credentials, ruling out (in order, each confirmed with the owner directly): stray whitespace in the Vercel env vars (added defensive `.trim()`), wrong appKey/appSecret value (re-copied via the EZVIZ console's own Copy button), Vercel env scoping, and an IP whitelist (no such setting exists on this account tier). Root cause never fully confirmed — owner asked to just point `token/get` directly at `isgpopen.ezvizlife.com` (this account's own regional domain, discovered via its `areaDomain` in an earlier successful curl) instead of the documented global entry point `open.ezvizlife.com`; this resolved it. `src/lib/ezviz.ts` now hardcodes this domain, no `EZVIZ_API_BASE` env var/fallback.
- Separately found and fixed a second, independent bug via the EZVIZ console's own "Address test" tool: the EZUIKit `ezopen://` URL must use `open.ezviz.com`, not `open.ys7.com` (guessed from public docs/examples) — this would have kept live-view broken even after the token/get fix.
- Live-view player was cramped (EZUIKit sized 640×400 inside the shared `Modal`'s 440px cap). Added an opt-in `wide` prop to `Modal` (other callers unaffected) and used it here, player resized to 800×480.
- Added `GET /api/cctv/hls` (per owner's spec): `token/get` → `v2/live/address/get` (protocol=2 HLS) → returns only `{url, expireTime}`. Admin-gated, not yet wired into `/admin/cctv`'s player (owner confirmed via `AskUserQuestion` to keep the working EZUIKit player as-is and add this as a standalone endpoint for a future `hls.js` player).
- **AI CCTV checkpoint module** (satpam/resepsionis presence detection): owner asked to build the AI-detection concept from the old unmerged branch (`claude/frigate-ai-cctv-module-eqwuri`) properly this time, routed through Mkhsistem's existing Gemini (not a second `GEMINI_API_KEY`). Confirmed 3 design decisions via `AskUserQuestion` before building: checkpoint every 2h (existing `checkpoint_interval_minutes` default), findings compiled into a **monthly** report per camera (not per-incident) that starts `pending_review` and needs an admin's confirm/dismiss before it's official (AI never issues the disciplinary verdict itself), dashboard-only visibility for now (no auto WA alert).
  - New table `cctv_disciplinary_reports` (reused the old branch's already-existing empty `cctv_checkpoint_log` table instead of recreating it).
  - `src/lib/ezviz.ts` gained `captureSnapshot()` (EZVIZ `device/capture`); `src/lib/aiBridge.ts` calls a new Mkhsistem endpoint (`app/api/villa/ai/cctv-vision`, pushed to Mkhsistem's own feature branch, **not yet deployed to Mkhsistem production** — pending owner's go-ahead) reusing the existing WA bridge's shared secret (`integration_settings.vercel_bridge`), so villa still holds no AI credential of its own.
  - `src/lib/cctvCheckpoint.ts` (per-camera + all-active-cameras runners, writes raw log rows) and `src/lib/cctvMonthlyReport.ts` (aggregates the prior month per camera into `cctv_disciplinary_reports`; never overwrites a report an admin already reviewed, even on rerun).
  - Two new Vercel Crons (`vercel.json`): `/api/cron/cctv-checkpoint` (every 2h) and `/api/cron/cctv-monthly-report` (1st of month), both gated by a new `CRON_SECRET` (owner still needs to set this in Vercel — the crons 503 until then). Plus `/api/cctv/run-now/[cameraId]` for a manual per-camera trigger.
  - villa-api v24: `/admin/cctv/cameras` POST/PATCH now accept `zona` (`satpam`/`resepsionis`/null) and `checkpoint_interval_minutes`; new `GET /admin/cctv/checkpoint-log` and `GET`+`PATCH /admin/cctv/disciplinary-reports` (PATCH is admin-only, sets `reviewed_by`/`reviewed_at`).
  - `/admin/cctv` page extended: zona/interval fields on the camera form, a "Run Now" button per zona-assigned camera, a Laporan Indisipliner section (list + review modal with confirm/dismiss + note), and a raw checkpoint log list.

### 2026-08-31 — Villa production deploy fix + Mkhsistem AI bridge deployed
- Villa's production deploy had silently stopped going out: the AI CCTV checkpoint module's `vercel.json` cron (every 2h) exceeded Vercel Hobby's daily-cron-only limit (`cron_jobs_limits_reached`), blocking every deploy attempt since it was added, even though the GitHub↔Vercel webhook itself was fine. Fixed by changing `cctv-checkpoint`'s schedule to once daily (04:00 UTC / 12:00 WITA); `main` deployed successfully immediately after.
- Mkhsistem's `app/api/villa/ai/cctv-vision` bridge endpoint (which `src/lib/aiBridge.ts` calls, pushed to a Mkhsistem feature branch on 2026-08-30/31 but not yet live per the note in the entry above) was implemented and deployed to Mkhsistem's production branch (`claude/mk-connect-app-o9zw2p`) — reuses `VILLA_BRIDGE_SECRET` (same secret already used by the existing WA bridge), no new credential. The AI CCTV checkpoint module should now get real Gemini presence-detection results instead of failing closed; not yet verified against a real EZVIZ snapshot.

### 2026-09-01 — Investor dividend bank account + automated tanggal-25 transfer list to admin
- Owner asked: investors should be able to fill in a bank account for monthly dividend transfers, and every tanggal 25 the system should automatically send the dividend list to Super Admin for processing the transfers.
- Read `villa-api`'s live v24 source directly before touching it (per the standing villa-api-verification rule) — confirmed `POST /me/investor-profile` (nama/hp only, first-login-only in practice since no page called it afterward) and `investor_profiles`/`villa_users` schema, then designed the addition to fit that exact existing pattern rather than inventing a new one.
- Migration `add_investor_bank_account_fields` (owner-approved, run from a separate session since this session's Supabase MCP access was auto-classifier-blocked for DDL): added `bank_nama`/`no_rekening`/`nama_pemilik_rekening` (all nullable) to `villa_users` and `investor_profiles`.
- villa-api v25: `POST /me/investor-profile` now also accepts the three bank fields (optional, backward-compatible); new `GET /me/investor-profile` (prefill), `GET /admin/dividends?periode=` (admin dividend list + rekening), `POST /cron/dividend-list` (cron-secret-gated, sends the WA list to every active admin, reusing `integration_settings.cron.secret` — no new secret).
- New `/investor/profil` page (view/edit rekening anytime, not just at first login); `admin/investors` table now shows each investor's rekening; new Vercel Cron `/api/cron/dividend-list` (tanggal 25, 09:00 WITA) calling the new villa-api cron endpoint. Verified via `tsc --noEmit` and `next build` before merging; merged to `main` and deployed same day.
- Not yet verified: an actual WhatsApp send to admins (cron hasn't fired yet as of this entry) — `GET /admin/dividends` can be called manually by an admin session to sanity-check the numbers ahead of the 25th.

### 2026-09-04 — Weekend surcharge + high-season/competitor-aware pricing (merged to `main` and deployed same day)
- Owner asked: weekend rates should go up Rp100.000 per room type, and high-season pricing should account for what nearby hotels/villas charge.
- Found and fixed a real gap while implementing this: the Revenue Engine's `villa_rates` table (written when an admin approves a recommendation) was never actually read at booking time — `POST /bookings` in `villa-api` priced strictly off the flat `units.tarif_harian`. **This is now fixed** (villa-api v31, deployed live to Supabase already, independent of this branch's Vercel deploy status): `POST /bookings` looks up `villa_rates` per night for `'harian'` bookings, falling back to `tarif_harian` when no rate is planned for that date. Does not touch existing bookings or `tarif_harian` itself.
- Extended `/api/cron/generate-pricing-recommendations`: on top of the existing occupancy rule, target dates that fall on Sat/Sun get a flat `+Rp100.000` ("weekend"); dates inside an active `villa_high_season_periods` row get floored at `current_rate * (1 + period.suggested_adjustment_pct)` ("high_season"), then floored again at the average of recent `villa_competitor_rates` for that room type if higher ("competitor_market_rate"). Still clamped to room-type min/max at the end; still lands as a `pending_review` recommendation an admin must approve — no new autonomous price-setting.
- New tables (migration `20260904150000_weekend_high_season_competitor_pricing`, applied): `villa_high_season_periods` (admin-defined date ranges + suggested %), `villa_competitor_rates` (observed nearby hotel/villa prices, `source` = `manual` or `ai_research`).
- New admin page `/admin/pricing-competitor`: manage high-season periods, add competitor rates manually, or trigger AI research. New Next.js routes: `/api/admin/high-season-periods` (CRUD), `/api/admin/competitor-rates` (list/add/delete), `/api/admin/competitor-rates/research` (calls the AI bridge, inserts results as `source='ai_research'` for review).
- AI research asks Gemini (Google Search grounding) for public nearby hotel/villa prices, per owner's explicit direction ("bisa mempelajari via google atau data umum yg tidak rahasia") — via a new Mkhsistem bridge (`app/api/villa/ai/competitor-pricing`, `lib/ai/domains/villa-competitor-pricing.ts`), reusing `VILLA_BRIDGE_SECRET` (no new credential). Same "AI reports, human reviews" discipline as the existing pricing-insight bridge — never sets a live price. Merged to Mkhsistem's production branch (`claude/mk-connect-app-o9zw2p`) same day.
- Verified via `tsc --noEmit` and `next build` (villa) and `tsc --noEmit` (Mkhsistem) before pushing. Merged directly (fast-forward, no conflicts) to both repos' production branches per owner's explicit "bawa ke production" instruction — no PR opened (direct-merge is this repo's observed historical pattern and no PR was requested).
